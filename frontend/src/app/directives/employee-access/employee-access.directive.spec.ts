import { EmployeeAccessDirective } from './employee-access.directive';

describe('EmployeeAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new EmployeeAccessDirective();
    expect(directive).toBeTruthy();
  });
});
