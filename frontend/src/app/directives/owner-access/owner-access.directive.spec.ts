import { OwnerAccessDirective } from './owner-access.directive';

describe('OwnerAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new OwnerAccessDirective();
    expect(directive).toBeTruthy();
  });
});
